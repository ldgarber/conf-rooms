<h1>Edit Meeting</h1>

<%= render 'form', meeting: @meeting %>

<!-- <%= link_to 'Show', @meeting %> | --> 
<%= link_to 'Back', root_path %> 
| <%= link_to "Delete", meeting_path(@meeting), method: :delete, data: {confirm: "Are you sure you want to delete this meeting?"} %> 

